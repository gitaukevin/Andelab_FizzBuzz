def find_max_min(sample_data):
	Min_Number= min (sample_data)
	Max_Number= max (sample_data)
	lists=[]
	if Min_Number == Max_Number:
	  return [len(sample_data)]
	else:  
	  return lists.append([Min_Number,Max_number])